services:
  database:
    image: postgres:16-alpine
    container_name: jerney-database
    restart: unless-stopped
    environment:
      POSTGRES_USER: jerney_user
      POSTGRES_PASSWORD: jerney-password-12212
      POSTGRES_DB: jerney_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data


    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jerney_user -d jerney_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    # Security: read-only root filesystem where possible

    tmpfs:
      - /tmp
      - /run
  
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: jerney-backend
    restart: unless-stopped

    environment:
      PORT: 5000
      DB_USER: jerney_user
      DB_PASSWORD: jerney-password-12212
      DB_HOST: jerney-database
      DB_PORT: 5432
      DB_NAME: jerney_db
    expose:
      - "5000"
    depends_on:
      database:
        condition: service_healthy

    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
      

  frontend:
    build:
      context: ./frontend
    container_name: jerney-frontend
    restart: unless-stopped
    ports:
      - "80:8080"
    depends_on:
      - backend
    security_opt:
      - no-new-privileges:true

volumes:
  pgdata:
    driver: local